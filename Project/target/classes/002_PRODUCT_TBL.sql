CREATE TABLE `project00`.`product_tbl` (
  `PRODUCT_INDEX` INT(8) NOT NULL,
  `PRODUCT_NAME` VARCHAR(45) NOT NULL,
  `REGISTRANT` VARCHAR(10) NOT NULL DEFAULT 'SYSTEM',
  `REGISTRATION_DATE` DATE NOT NULL DEFAULT ,
  `DELETE_FLAG` INT(1) NOT NULL DEFAULT 0,
  `PRICE` DECIMAL(10) NOT NULL DEFAULT 0,
  `DISCOUNT` DECIMAL(3) NOT NULL DEFAULT 0,
  `PRODUCT_MENUAL` VARCHAR(256) CHARACTER SET 'utf8' COLLATE 'utf8_bin' NULL,
  PRIMARY KEY (`PRODUCT_INDEX`))
COMMENT = '상품 테이블';
